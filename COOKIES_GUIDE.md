# 쿠키 인증 설정 가이드 (429 차단 해결)

> **✅ 권장: Firefox 방식 (가장 쉬움, 갱신 불필요)**
>
> 1. [Firefox 설치](https://www.mozilla.org/firefox/) (평소 쓰는 브라우저는 그대로)
> 2. Firefox에서 youtube.com에 로그인 (멤버십 있으면 그 계정으로)
> 3. 끝 — `yt.sh`가 Firefox 프로필을 자동 감지해 매 실행 최신 쿠키를 직접 읽습니다.
>    내보내기·갱신 작업이 없고, 쿠키 회전으로 몇 시간 만에 만료되는 문제도 없습니다.
>    (세션이 아예 만료된 경우에만 Firefox를 열어 재로그인)
>
> 아래의 cookies.txt 수동 내보내기는 Firefox를 쓸 수 없을 때의 대안입니다.

YouTube가 요청 과다(HTTP 429)로 IP를 차단하는 것을 막기 위해, 로그인된 브라우저의 쿠키를 사용합니다.

> **비밀번호는 필요 없습니다.** 이미 로그인된 상태의 쿠키만 파일로 내보냅니다.

---

## 1단계 — 쿠키 내보내기 확장프로그램 설치

크롬 웹스토어에서 설치:

```
"Get cookies.txt LOCALLY"  검색 → 설치
```

> 이 확장프로그램은 쿠키를 외부 전송 없이 **로컬에서만** 추출합니다 (이름의 LOCALLY).

---

## 2단계 — YouTube 쿠키 내보내기

```
1. 크롬에서 youtube.com 접속 (이미 로그인된 상태 그대로)
2. 우측 상단 확장프로그램 아이콘 클릭
3. "Get cookies.txt LOCALLY" 선택
4. [Export] 또는 [Download] 클릭
5. cookies.txt 파일이 다운로드됨
```

---

## 3단계 — 프로젝트 폴더에 배치

```bash
# 다운로드한 cookies.txt를 프로젝트 폴더로 이동
mv ~/Downloads/cookies.txt ~/yt-subs/cookies.txt

# 확인
ls -la ~/yt-subs/cookies.txt
```

---

## 4단계 — 실행 (쿠키 자동 인식)

```bash
cd ~/yt-subs
./yt.sh run
```

쿠키가 인식되면 실행 시 이 메시지가 보입니다:
```
▢ 쿠키 인증 사용: cookies.txt
```

---

## 동작 방식

| cookies.txt 상태 | 동작 |
|---|---|
| 있음 | 로그인 인증 → 429 대폭 감소 |
| 없음 | 비로그인 폴백 (429 발생 가능) |

---

## 보안 주의

| 항목 | 내용 |
|---|---|
| 민감도 | 쿠키 = 비밀번호급 인증 정보 |
| 보관 | 본인 PC에만, 공유·업로드·git 커밋 금지 |
| 유효기간 | 만료되면 다시 내보내기 (2단계 반복) |
| 유출 시 | 구글 계정 → 보안 → 모든 기기 로그아웃하면 무효화 |

> **권장:** 메인 계정이 부담되면 무료 서브 구글 계정을 만들어 그 계정으로 로그인 후 쿠키를 내보내세요. 메인 계정이 보호됩니다.

---

## 쿠키가 금방 만료되는 경우 (중요)

YouTube는 **열려 있는 브라우저 세션의 쿠키를 수시로 회전**시킵니다.
쿠키를 내보낸 뒤 같은 브라우저에서 유튜브를 계속 쓰면, 내보낸 파일이
몇 시간 만에 무효화될 수 있습니다 (실제 사례: 06:08 내보내기 → 08:11 무효 감지).

**yt-dlp 공식 권장 절차 — 시크릿 창 방식:**

```
1. 크롬 시크릿 창(Cmd+Shift+N)을 열고 youtube.com에 로그인
   (멤버십 가입 계정인지 확인!)
2. 그 시크릿 창에서 "Get cookies.txt LOCALLY"로 쿠키 내보내기
3. 로그아웃하지 말고 시크릿 창을 그냥 닫기
4. mv ~/Downloads/cookies.txt ~/yt-subs/cookies.txt
```

이렇게 하면 그 세션의 쿠키가 브라우저에 의해 회전되지 않아 오래 유지됩니다.
쿠키 상태는 대시보드 하단 또는 `curl localhost:8800/cookies`로 확인할 수 있습니다
(⚠ 경고가 보이면 재추출 필요).

---

## 멤버십 영상 추출 조건

1. 쿠키가 유효할 것 (위 시크릿 창 방식 권장)
2. 쿠키의 계정이 **해당 채널 멤버십에, 영상이 요구하는 등급 이상으로** 가입되어 있을 것
   (예: "Gold Class or higher" 영상은 하위 등급 가입으로는 접근 불가)
3. 대시보드 조건 추출 시 **"멤버십 전용 포함"을 체크**할 것 (기본 꺼짐)
   — CLI `run`은 쿠키가 있으면 자동으로 재시도하므로 별도 설정 불필요

---

## 429가 계속 발생하면

연속 429가 5회 발생하면 자동으로 중단되고 안내가 나옵니다:
```
⛔ 연속 429 차단 5회 → 추출을 중단했습니다.
💡 30분~1시간 후 './yt.sh run' 으로 이어받기 하세요.
```

이미 받은 영상은 `state.json`에 기록되어 있어, 차단이 풀린 뒤 `./yt.sh run`을 다시 실행하면 **중단된 지점부터 이어받습니다.**

---

## .gitignore 권장

쿠키 파일이 실수로 커밋되지 않도록:
```bash
echo "cookies.txt" >> ~/yt-subs/.gitignore
```
